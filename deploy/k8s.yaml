apiVersion: apps/v1
kind: Deployment
metadata: {name: noliae-search}
spec:
  replicas: 2
  selector: {matchLabels: {app: noliae-search}}
  template:
    metadata: {labels: {app: noliae-search}}
    spec:
      containers:
        - name: web
          image: ghcr.io/noliae-france/noliae-search:main
          ports: [{containerPort: 8080}]
          readinessProbe: {httpGet: {path: /api/health, port: 8080}}
          livenessProbe: {httpGet: {path: /api/health, port: 8080}}
---
apiVersion: v1
kind: Service
metadata: {name: noliae-search}
spec:
  selector: {app: noliae-search}
  ports: [{port: 80, targetPort: 8080}]
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: {name: noliae-search}
spec:
  rules:
    - host: noliae.com
      http: {paths: [{path: /, pathType: Prefix, backend: {service: {name: noliae-search, port: {number: 80}}}}]}
    - host: www.noliae.com
      http: {paths: [{path: /, pathType: Prefix, backend: {service: {name: noliae-search, port: {number: 80}}}}]}
